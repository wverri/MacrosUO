function autoWeaponAbility() {
	while (true) {
	    if (!Orion.AbilityStatus('Primary') && Player.Mana() > 20) {
	        Orion.UseAbility('Primary');
	        Orion.Say('[set4');
	        Orion.Wait(100);
	    } 
	    Orion.Wait(500);
	}
}